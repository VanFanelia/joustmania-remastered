<svg width="150" height="200" viewBox="0 0 150 200" xmlns="http://www.w3.org/2000/svg" fill="none">
    <defs>
        <!-- cutout mask for button -->
        <mask id="cutout">
            <rect width="150" height="220" fill="white"/>
            <circle cx="75" cy="90" r="5" fill="black"/>
        </mask>
    </defs>
    <g transform="rotate(30 75 100)">
        <!-- coloredHead -->
        <circle cx="75" cy="30" r="20" fill="#ffa500" stroke="currentColor" stroke-width="4"/>

        <!-- Kürzerer Griff -->
        <rect x="60" y="50" width="30" height="90" rx="15" fill="currentColor" stroke="currentColor" stroke-width="2" mask="url(#cutout)"/>
    </g>
</svg>

